# My Application

This application provides REST API services.
The debug mode is enabled during development.
Server runs on port 8080 by default.

## Setup
Install dependencies with pip install -r requirements.txt
Run with python app.py
The server is configured for debug mode.
